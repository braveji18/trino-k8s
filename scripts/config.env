# Trino Runtime 배포 설정값
# install.sh가 source 하여 사용. 환경변수로 override 가능.
#   예) NAMESPACE=other-ns ./scripts/install.sh

# 배포 대상 Kubernetes namespace
NAMESPACE="${NAMESPACE:-user-braveji}"

# Helm release 이름
RELEASE_NAME="${RELEASE_NAME:-my-trino}"
